@model proiektua2.Models.Saskia

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>
@{

    if (AdminDbContext.user.tipo == "admin")
    {
        <div>
            <h4>Saskia</h4>
            <hr />
            <dl class="row">
            </dl>
        </div>
        <div>
            <a asp-action="Edit" asp-route-id="@Model.SaskiaId">Edit</a> |
            <a asp-action="Index">Back to List</a>
        </div>
        }
    }