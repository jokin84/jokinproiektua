@model proiektua2.Models.Saskia

@{
    ViewData["Title"] = "Create";
}
@{

    if (AdminDbContext.user.tipo == "admin")
    {
        <h1>Create</h1>

        <h4>Saskia</h4>
        <hr />
        <div class="row">
            <div class="col-md-4">
                <form asp-action="Create">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group">
                        <label asp-for="SaskiaId" class="control-label"></label>
                        <input asp-for="SaskiaId" class="form-control" />
                        <span asp-validation-for="SaskiaId" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <input type="submit" value="Create" class="btn btn-primary" />
                    </div>
                </form>
            </div>
        </div>

        <div>
            <a asp-action="Index">Back to List</a>
        </div>
        }
    }
